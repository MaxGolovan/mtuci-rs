// variables5.rs
// Execute `rustlings hint variables5` or use the `hint` watch subcommand for a hint.



fn main() {
    let number_text = "T-H-R-E-E"; 
    println!("Spell a Number: {}", number_text);
    let number = 3; 
    println!("Number plus two is: {}", number + 2);
}
